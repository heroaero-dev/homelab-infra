apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: linkding
spec:
  ingressClassName: traefik
  rules:
    - host: link.heroaero.dev
      http:
        paths:
          - backend:
              service:
                name: linkding
                port:
                  number: 9000
            path: /
            pathType: Prefix
