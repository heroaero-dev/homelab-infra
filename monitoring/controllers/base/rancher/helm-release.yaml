apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rancher
  namespace: cattle-system
spec:
  interval: 10m
  chart:
    spec:
      chart: rancher
      version: "2.11.3"
      sourceRef:
        kind: HelmRepository
        name: rancher
        namespace: flux-system
      interval: 10m
  values:
    replicaCount: 2
